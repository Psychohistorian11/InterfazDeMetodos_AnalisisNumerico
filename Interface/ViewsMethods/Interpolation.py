import tkinter as tk


class Interpolation:

    def __init__(self, root):
        window = tk.Toplevel(root)
        window.title("Interpolación y Ajuste")
        label = tk.Label(window, text="Esta es la ventana de Interpolación y AjusteCeros")
        label.pack(pady=10)
