import tkinter as tk

class Zeros:

    def __init__(self, root):
        window = tk.Toplevel(root)
        window.title("Ceros de funciones")
        label = tk.Label(window, text="Esta es la ventana de Ceros de funciones")
        label.pack(pady=10)